plugins {
  id "org.sonarqube" version "3.5.0.2730"
}

sonarqube {
  properties {
    property "sonar.projectKey", "vbkuzhel_sonar-gpt-fixes"
    property "sonar.organization", "vbkuzhel"
    property "sonar.host.url", "https://sonarcloud.io"
  }
}